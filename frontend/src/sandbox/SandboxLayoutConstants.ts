/**
 * Re-export editor panel layout so Sandbox matches Workflow Editor constraints
 * without scattering magic numbers.
 */
export {
    CENTER_PANEL_MIN_PX,
    clampPanelWidths,
    DEFAULT_LEFT_PANEL_WIDTH_PX,
    DEFAULT_RIGHT_PANEL_WIDTH_PX,
    LEFT_PANEL_MIN_PX,
    PALETTE_SECTION_LIST_MAX_HEIGHT_CLASS,
    RIGHT_PANEL_MIN_PX,
} from '../components/workflow-editor/workflowEditorPanelLayout';
