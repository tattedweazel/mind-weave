# Skill Explorer UI (deprecated)

This document described **`details.skill_explorer`**, which has been **replaced** by **`details.output_explorer`** (same versioned shape and UX patterns, extended to all node kinds).

**Current documentation:** **[OUTPUT_EXPLORER_UI.md](OUTPUT_EXPLORER_UI.md)**

The app still **reads** legacy **`skill_explorer`** on old run logs via `parseEffectiveOutputExplorer` in the frontend.
