STT_BRIDGE_TOKEN=
STT_BRIDGE_MOCK=false
STT_MODEL=medium
STT_DEVICE=auto
STT_COMPUTE_TYPE=default

# Max accepted audio upload, in bytes. Keep aligned with backend
# STT_MAX_AUDIO_UPLOAD_BYTES, frontend VITE_STT_MAX_AUDIO_UPLOAD_BYTES,
# and nginx client_max_body_size.
STT_MAX_AUDIO_BYTES=78643200
